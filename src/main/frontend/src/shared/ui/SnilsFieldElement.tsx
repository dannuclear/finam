import { TextFieldElement, type TextFieldElementProps } from 'react-hook-form-mui';

export const SnilsFieldElement = (props: TextFieldElementProps) => {
    return (
        <TextFieldElement {...props} />
    )
}
